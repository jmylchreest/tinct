// niri colour configuration example generated by Tinct
// https://github.com/jmylchreest/tinct
//
// This file demonstrates how to wire the Tinct colours into your niri config.
// The real work is done by {{ .ColorFileName }}, which sets niri's themeable
// colour properties directly. To apply it, add this line near the END of your
// ~/.config/niri/config.kdl (after your own `layout` block so the merge wins):
//
//   include "{{ .ColorFileName }}"
//
// niri includes are positional and merge per-property: only the properties set
// in the included file change, everything else in your config is preserved.
// Included files are watched, so niri live-reloads when Tinct rewrites them.
//
// ----------------------------------------------------------------------------
// Detected theme: {{ themeType . }}
//
// The values below are the same ones written into {{ .ColorFileName }}; this
// block is a read-only reference so you can see what gets applied. You normally
// do NOT include this file - include {{ .ColorFileName }} instead.
// ----------------------------------------------------------------------------
//
// layout {
//     focus-ring {
//         active-gradient from="{{ get . "accent1" | hex }}" to="{{ get . "accent2" | hex }}" angle=45
//         inactive-color "{{ get . "backgroundMuted" | hex }}"
//     }
//     border {
//         active-color "{{ get . "accent1" | hex }}"
//         inactive-color "{{ get . "backgroundMuted" | hex }}"
//     }
//     insert-hint { color "{{ get . "accent2" | hex }}" }
//     tab-indicator {
//         active-color "{{ get . "accent1" | hex }}"
//         inactive-color "{{ get . "backgroundMuted" | hex }}"
//     }
//     shadow { color "{{ get . "shadow" | hexAlpha }}" }
// }
// overview { backdrop-color "{{ get . "scrim" | hex }}" }
//
// ----------------------------------------------------------------------------
// Available colour roles (use `get . "<role>" | hex`):
//
//   Core:      background backgroundMuted foreground foregroundMuted
//   Accents:   accent1 accent2 accent3 accent4 (+ *Muted variants)
//   Semantic:  danger warning success info notification
//   Surface:   surface onSurface outline border borderMuted
//   Misc:      scrim shadow
//
//   Hex with alpha (for shadow/scrim): `get . "shadow" | hexAlpha`
// ----------------------------------------------------------------------------
