# Hyprlock colour theme generated by Tinct
# https://github.com/jmylchreest/tinct
#
# Include this file in your hyprlock.conf with:
#   source = ~/.config/hypr/tinct-hyprlock.conf
#
# Or copy the color variables to your existing hyprlock.conf
#
# Detected theme: {{ .SourceTheme }}

# ============================================================================
# Tinct Color Variables
# ============================================================================

$tinct_background = rgb({{ .BackgroundRGB }})
$tinct_background_muted = rgb({{ .BackgroundMutedRGB }})
$tinct_foreground = rgb({{ .ForegroundRGB }})
$tinct_foreground_muted = rgb({{ .ForegroundMutedRGB }})

$tinct_accent1 = rgb({{ .Accent1RGB }})
$tinct_accent2 = rgb({{ .Accent2RGB }})
$tinct_accent3 = rgb({{ .Accent3RGB }})
$tinct_accent4 = rgb({{ .Accent4RGB }})

$tinct_danger = rgb({{ .DangerRGB }})
$tinct_warning = rgb({{ .WarningRGB }})
$tinct_success = rgb({{ .SuccessRGB }})
$tinct_info = rgb({{ .InfoRGB }})

# ============================================================================
# RGBA Variants (with alpha channel)
# ============================================================================

$tinct_background_rgba = rgba({{ .BackgroundRGBA "ee" }})
$tinct_background_muted_rgba = rgba({{ .BackgroundMutedRGBA "cc" }})
$tinct_foreground_rgba = rgba({{ .ForegroundRGBA "ff" }})
$tinct_foreground_muted_rgba = rgba({{ .ForegroundMutedRGBA "dd" }})

$tinct_accent1_rgba = rgba({{ .Accent1RGBA "ff" }})
$tinct_accent2_rgba = rgba({{ .Accent2RGBA "ff" }})
$tinct_accent3_rgba = rgba({{ .Accent3RGBA "ff" }})
$tinct_accent4_rgba = rgba({{ .Accent4RGBA "ff" }})

$tinct_danger_rgba = rgba({{ .DangerRGBA "ff" }})
$tinct_warning_rgba = rgba({{ .WarningRGBA "ff" }})
$tinct_success_rgba = rgba({{ .SuccessRGBA "ff" }})
$tinct_info_rgba = rgba({{ .InfoRGBA "ff" }})

# ============================================================================
# Usage Examples (commented out - uncomment to use)
# ============================================================================

# background {
#     monitor =
#     path = screenshot
#     color = $tinct_background
#     blur_passes = 3
# }

# input-field {
#     monitor =
#     size = 20%, 5%
#     outline_thickness = 3
#
#     inner_color = $tinct_background_rgba
#     outer_color = $tinct_accent1_rgba
#     check_color = $tinct_success_rgba
#     fail_color = $tinct_danger_rgba
#
#     font_color = $tinct_foreground
#     placeholder_text = <i>Input password...</i>
#     fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
#
#     position = 0, -20
#     halign = center
#     valign = center
# }

# label {
#     monitor =
#     text = $TIME
#     font_size = 90
#     color = $tinct_foreground
#
#     position = -30, 0
#     halign = right
#     valign = top
# }

# label {
#     monitor =
#     text = cmd[update:60000] date +"%A, %d %B %Y"
#     font_size = 25
#     color = $tinct_foreground_muted
#
#     position = -30, -150
#     halign = right
#     valign = top
# }

# ============================================================================
# Color Reference (for customization)
# ============================================================================
# Background:      {{ .Background }}
# Foreground:      {{ .Foreground }}
# BackgroundMuted: {{ .BackgroundMuted }}
# ForegroundMuted: {{ .ForegroundMuted }}
# Accent1:         {{ .Accent1 }}
# Accent2:         {{ .Accent2 }}
# Accent3:         {{ .Accent3 }}
# Accent4:         {{ .Accent4 }}
# Danger:          {{ .Danger }}
# Warning:         {{ .Warning }}
# Success:         {{ .Success }}
# Info:            {{ .Info }}
