# Hyprlock colour theme generated by Tinct
# https://github.com/jmylchreest/tinct
#
# Include this file in your hyprlock.conf with:
#   source = ~/.config/hypr/tinct-hyprlock.conf
#
# Or copy the color variables to your existing hyprlock.conf
#
# Detected theme: {{ themeType .PaletteHelper }}

# ============================================================================
# Tinct Wallpaper Variable
# ============================================================================
{{- if .WallpaperPath }}

$tinct_wallpaper = {{ .WallpaperPath }}
{{- end }}

# ============================================================================
# Tinct Color Variables
# ============================================================================

$tinct_background = rgb({{ get .PaletteHelper "background" | rgbDecimal }})
$tinct_background_muted = rgb({{ get .PaletteHelper "backgroundMuted" | rgbDecimal }})
$tinct_foreground = rgb({{ get .PaletteHelper "foreground" | rgbDecimal }})
$tinct_foreground_muted = rgb({{ get .PaletteHelper "foregroundMuted" | rgbDecimal }})

$tinct_accent1 = rgb({{ get .PaletteHelper "accent1" | rgbDecimal }})
$tinct_accent2 = rgb({{ get .PaletteHelper "accent2" | rgbDecimal }})
$tinct_accent3 = rgb({{ get .PaletteHelper "accent3" | rgbDecimal }})
$tinct_accent4 = rgb({{ get .PaletteHelper "accent4" | rgbDecimal }})

$tinct_danger = rgb({{ get .PaletteHelper "danger" | rgbDecimal }})
$tinct_warning = rgb({{ get .PaletteHelper "warning" | rgbDecimal }})
$tinct_success = rgb({{ get .PaletteHelper "success" | rgbDecimal }})
$tinct_info = rgb({{ get .PaletteHelper "info" | rgbDecimal }})

# ============================================================================
# RGBA Variants (with alpha channel)
# ============================================================================

$tinct_background_rgba = rgba({{ withAlpha (get .PaletteHelper "background") 0.93 | rgbDecimal }})
$tinct_background_muted_rgba = rgba({{ withAlpha (get .PaletteHelper "backgroundMuted") 0.80 | rgbDecimal }})
$tinct_foreground_rgba = rgba({{ get .PaletteHelper "foreground" | rgbDecimal }})
$tinct_foreground_muted_rgba = rgba({{ withAlpha (get .PaletteHelper "foregroundMuted") 0.87 | rgbDecimal }})

$tinct_accent1_rgba = rgba({{ get .PaletteHelper "accent1" | rgbDecimal }})
$tinct_accent2_rgba = rgba({{ get .PaletteHelper "accent2" | rgbDecimal }})
$tinct_accent3_rgba = rgba({{ get .PaletteHelper "accent3" | rgbDecimal }})
$tinct_accent4_rgba = rgba({{ get .PaletteHelper "accent4" | rgbDecimal }})

$tinct_danger_rgba = rgba({{ get .PaletteHelper "danger" | rgbDecimal }})
$tinct_warning_rgba = rgba({{ get .PaletteHelper "warning" | rgbDecimal }})
$tinct_success_rgba = rgba({{ get .PaletteHelper "success" | rgbDecimal }})
$tinct_info_rgba = rgba({{ get .PaletteHelper "info" | rgbDecimal }})

# ============================================================================
# Usage Examples (commented out - uncomment to use)
# ============================================================================

# background {
#     monitor =
{{- if .WallpaperPath }}
#     path = $tinct_wallpaper
{{- else }}
#     path = screenshot
{{- end }}
#     color = $tinct_background
#     blur_passes = 3
# }

# input-field {
#     monitor =
#     size = 20%, 5%
#     outline_thickness = 3
#
#     inner_color = $tinct_background_rgba
#     outer_color = $tinct_accent1_rgba
#     check_color = $tinct_success_rgba
#     fail_color = $tinct_danger_rgba
#
#     font_color = $tinct_foreground
#     placeholder_text = <i>Input password...</i>
#     fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
#
#     position = 0, -20
#     halign = center
#     valign = center
# }

# label {
#     monitor =
#     text = $TIME
#     font_size = 90
#     color = $tinct_foreground
#
#     position = -30, 0
#     halign = right
#     valign = top
# }

# label {
#     monitor =
#     text = cmd[update:60000] date +"%A, %d %B %Y"
#     font_size = 25
#     color = $tinct_foreground_muted
#
#     position = -30, -150
#     halign = right
#     valign = top
# }

# ============================================================================
# Color Reference (for customization)
# ============================================================================
# Background:      {{ get .PaletteHelper "background" | hex }}
# Foreground:      {{ get .PaletteHelper "foreground" | hex }}
# BackgroundMuted: {{ get .PaletteHelper "backgroundMuted" | hex }}
# ForegroundMuted: {{ get .PaletteHelper "foregroundMuted" | hex }}
# Accent1:         {{ get .PaletteHelper "accent1" | hex }}
# Accent2:         {{ get .PaletteHelper "accent2" | hex }}
# Accent3:         {{ get .PaletteHelper "accent3" | hex }}
# Accent4:         {{ get .PaletteHelper "accent4" | hex }}
# Danger:          {{ get .PaletteHelper "danger" | hex }}
# Warning:         {{ get .PaletteHelper "warning" | hex }}
# Success:         {{ get .PaletteHelper "success" | hex }}
# Info:            {{ get .PaletteHelper "info" | hex }}
