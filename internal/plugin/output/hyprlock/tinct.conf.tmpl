# Hyprlock colour theme generated by Tinct
# https://github.com/jmylchreest/tinct
#
# Include this file in your hyprlock.conf with:
#   source = ~/.config/hypr/tinct-hyprlock.conf
#
# Or copy the color variables to your existing hyprlock.conf
#
# Detected theme: {{ themeType . }}

# ============================================================================
# Tinct Wallpaper Variable
# ============================================================================
{{- if .WallpaperPath }}

$tinct_wallpaper = {{ .WallpaperPath }}
{{- end }}

# ============================================================================
# Tinct Color Variables (RGB format)
# ============================================================================

$tinct_background = rgb({{ get . "background" | rgbDecimal }})
$tinct_background_muted = rgb({{ get . "backgroundMuted" | rgbDecimal }})
$tinct_foreground = rgb({{ get . "foreground" | rgbDecimal }})
$tinct_foreground_muted = rgb({{ get . "foregroundMuted" | rgbDecimal }})

$tinct_accent1 = rgb({{ get . "accent1" | rgbDecimal }})
$tinct_accent2 = rgb({{ get . "accent2" | rgbDecimal }})
$tinct_accent3 = rgb({{ get . "accent3" | rgbDecimal }})
$tinct_accent4 = rgb({{ get . "accent4" | rgbDecimal }})

$tinct_danger = rgb({{ get . "danger" | rgbDecimal }})
$tinct_warning = rgb({{ get . "warning" | rgbDecimal }})
$tinct_success = rgb({{ get . "success" | rgbDecimal }})
$tinct_info = rgb({{ get . "info" | rgbDecimal }})

# ============================================================================
# Hex Variants (without # prefix, for hyprlock compatibility)
# ============================================================================

$tinct_background_hex = {{ get . "background" | hexNoHash }}
$tinct_background_muted_hex = {{ get . "backgroundMuted" | hexNoHash }}
$tinct_foreground_hex = {{ get . "foreground" | hexNoHash }}
$tinct_foreground_muted_hex = {{ get . "foregroundMuted" | hexNoHash }}

$tinct_accent1_hex = {{ get . "accent1" | hexNoHash }}
$tinct_accent2_hex = {{ get . "accent2" | hexNoHash }}
$tinct_accent3_hex = {{ get . "accent3" | hexNoHash }}
$tinct_accent4_hex = {{ get . "accent4" | hexNoHash }}

$tinct_danger_hex = {{ get . "danger" | hexNoHash }}
$tinct_warning_hex = {{ get . "warning" | hexNoHash }}
$tinct_success_hex = {{ get . "success" | hexNoHash }}
$tinct_info_hex = {{ get . "info" | hexNoHash }}

# ============================================================================
# RGBA Variants (with alpha channel)
# ============================================================================

$tinct_background_rgba = rgba({{ withAlpha (get . "background") 0.93 | rgbaDecimal }})
$tinct_background_muted_rgba = rgba({{ withAlpha (get . "backgroundMuted") 0.80 | rgbaDecimal }})
$tinct_foreground_rgba = rgba({{ get . "foreground" | rgbaDecimal }})
$tinct_foreground_muted_rgba = rgba({{ withAlpha (get . "foregroundMuted") 0.87 | rgbaDecimal }})

$tinct_accent1_rgba = rgba({{ get . "accent1" | rgbaDecimal }})
$tinct_accent2_rgba = rgba({{ get . "accent2" | rgbaDecimal }})
$tinct_accent3_rgba = rgba({{ get . "accent3" | rgbaDecimal }})
$tinct_accent4_rgba = rgba({{ get . "accent4" | rgbaDecimal }})

$tinct_danger_rgba = rgba({{ get . "danger" | rgbaDecimal }})
$tinct_warning_rgba = rgba({{ get . "warning" | rgbaDecimal }})
$tinct_success_rgba = rgba({{ get . "success" | rgbaDecimal }})
$tinct_info_rgba = rgba({{ get . "info" | rgbaDecimal }})

# ============================================================================
# Usage Examples (commented out - uncomment to use)
# ============================================================================

# background {
#     monitor =
{{- if .WallpaperPath }}
#     path = $tinct_wallpaper
{{- else }}
#     path = screenshot
{{- end }}
#     color = $tinct_background
#     blur_passes = 3
# }

# input-field {
#     monitor =
#     size = 20%, 5%
#     outline_thickness = 3
#
#     inner_color = $tinct_background_rgba
#     outer_color = $tinct_accent1_rgba
#     check_color = $tinct_success_rgba
#     fail_color = $tinct_danger_rgba
#
#     font_color = $tinct_foreground
#     placeholder_text = <i>Input password...</i>
#     fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
#
#     position = 0, -20
#     halign = center
#     valign = center
# }

# label {
#     monitor =
#     text = $TIME
#     font_size = 90
#     color = $tinct_foreground
#
#     position = -30, 0
#     halign = right
#     valign = top
# }

# label {
#     monitor =
#     text = cmd[update:60000] date +"%A, %d %B %Y"
#     font_size = 25
#     color = $tinct_foreground_muted
#
#     position = -30, -150
#     halign = right
#     valign = top
# }

# ============================================================================
# Color Reference (for customization)
# ============================================================================
# Background:      {{ get . "background" | hex }}
# Foreground:      {{ get . "foreground" | hex }}
# BackgroundMuted: {{ get . "backgroundMuted" | hex }}
# ForegroundMuted: {{ get . "foregroundMuted" | hex }}
# Accent1:         {{ get . "accent1" | hex }}
# Accent2:         {{ get . "accent2" | hex }}
# Accent3:         {{ get . "accent3" | hex }}
# Accent4:         {{ get . "accent4" | hex }}
# Danger:          {{ get . "danger" | hex }}
# Warning:         {{ get . "warning" | hex }}
# Success:         {{ get . "success" | hex }}
# Info:            {{ get . "info" | hex }}
